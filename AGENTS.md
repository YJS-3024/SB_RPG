# AGENTS.md

## 목적

이 파일은 Codex/AI가 이 Unity 프로젝트에서 작업할 때 지켜야 할 공통 규칙이다.

핵심 목표:

- 불필요한 파일 읽기 줄이기
- 토큰 사용량 절약
- 설계 문서 기반으로 일관성 유지
- 사용자가 직접 설계하고 싶은 큰 구조를 AI가 과하게 대신 구현하지 않기

## 토큰 절약 규칙

- 작업 시작 시 전체 프로젝트를 스캔하지 않는다.
- 먼저 사용자 요청과 직접 관련된 파일명, 심볼, 에러 메시지만 검색한다.
- 검색은 가능한 `rg`를 우선 사용한다.
- `Library/`, `Temp/`, `Logs/`, `obj/`, `bin/`, `.vs/`, `.idea/`, `UserSettings/`, `Build/`, `Builds/`는 요청이 없으면 읽지 않는다.
- 대형 에셋 파일은 열지 않는다.
- Unity 생성 파일인 `*.csproj`, `*.sln`, `*.slnx`는 빌드/참조 문제 확인이 필요할 때만 읽는다.
- 큰 파일은 전체를 읽지 말고 관련 클래스, 메서드, 에러 라인 주변만 읽는다.
- 검색 결과가 많으면 폴더, 확장자, 정확한 심볼명으로 다시 좁힌다.
- 변경하지 않은 파일은 최종 답변에서 길게 설명하지 않는다.
- 최종 답변은 변경 파일, 변경 이유, 검증 결과만 짧게 정리한다.

## 경로 표기 토큰 절약 규칙

- 현재 작업 루트가 확인된 뒤에는 같은 루트의 절대 경로를 반복하지 않는다.
- 분석, 진행 상황, 최종 답변에서는 가능하면 상대 경로를 사용한다.
- 예: `E:\Backup\Project\unity\Project\Unity 6\SB_RPG\Assets\Scripts\Player\PlayerFSM.cs` 대신 `Assets/Scripts/Player/PlayerFSM.cs`로 쓴다.
- 같은 폴더의 파일을 여러 개 언급할 때는 폴더명을 한 번만 쓰고 파일명만 나열한다.
- 파일 링크가 꼭 필요한 UI 응답에서만 절대 경로를 사용한다.
- 명령 실행 결과의 긴 절대 경로는 의미가 바뀌지 않으면 프로젝트 루트 기준 상대 경로로 요약한다.
- Windows 역슬래시 경로는 설명 문장에서는 `/` 기반 상대 경로로 줄여 쓴다.
- 동일한 파일 경로를 한 답변에서 여러 번 반복하지 않는다.
- 에러 메시지에 긴 경로가 포함되면 파일명, 상대 경로, 라인 번호만 남긴다.
- 경로를 설명하기 위해 디렉터리 트리 전체를 출력하지 않는다.
- 현재 작업 루트 자체는 필요할 때 한 번만 언급하고 이후에는 `프로젝트 루트`라고 부른다.
- 여러 파일이 같은 폴더에 있으면 공통 prefix를 반복하지 않는다.
  - 나쁜 예: `Assets/Scripts/Combat/A.cs`, `Assets/Scripts/Combat/B.cs`, `Assets/Scripts/Combat/C.cs`
  - 좋은 예: `Assets/Scripts/Combat/`의 `A.cs`, `B.cs`, `C.cs`
- Unity 프로젝트 내부 파일은 가능하면 `Assets/...`, `Packages/...`, `ProjectSettings/...`, `설계/...` 형태로 표기한다.
- 외부 임시 파일이나 시스템 경로는 사용자에게 꼭 필요한 경우에만 절대 경로로 표기한다.
- 파일 이동/삭제/생성 결과를 말할 때도 전체 경로 대신 프로젝트 기준 경로를 사용한다.
- 같은 경로를 코드블록, 문장, 목록에 중복해서 쓰지 않는다.
- 명령 예시는 이미 `workdir`가 프로젝트 루트라고 가정하고 짧게 쓴다.
  - 예: `rg "PlayerFSM" Assets`
  - 피할 것: `rg "PlayerFSM" "E:\...\SB_RPG\Assets"`
- Unity 로그나 컴파일러 출력에서 같은 긴 경로가 반복되면 첫 1회만 상대 경로로 변환해 언급하고 나머지는 생략한다.
- 경로 목록을 보여줘야 할 때는 최대한 관련 파일만 3~10개로 제한한다.

## 추가 토큰 절약 규칙

- `설계/` 문서는 전체를 매번 읽지 말고 요청과 직접 관련된 문서 1~2개만 읽는다.
- `.unity`, `.prefab`, `.asset`, `.mat`, `.controller`, `.anim` 파일은 참조 문제나 명시 요청이 있을 때만 읽는다.
- Unity YAML 파일을 읽을 때는 전체 파일을 출력하지 말고 필요한 GUID, 컴포넌트명, 참조 라인만 검색한다.
- `Logs/Editor.log`는 전체를 읽지 말고 `error CS`, `Exception`, `NullReferenceException`, 대상 파일명으로 검색한다.
- 에셋 파일은 먼저 파일명, 경로, import 설정, `.meta` 여부만 확인한다.
- 최종 답변에는 명령 출력 전문을 붙이지 않고 결과만 요약한다.
- 변경하지 않은 파일은 최종 답변에서 언급하지 않는다.
- `rg` 결과는 필요한 만큼만 확인하고 전체 결과를 답변에 붙이지 않는다.
- 넓은 검색은 `Assets/**/*.cs`, `Packages/`, `설계/`처럼 폴더와 확장자를 제한한다.
- 같은 실패 검증을 여러 번 반복하지 않는다.
- 환경 문제로 검증이 실패하면 원인과 다음 확인 방법만 짧게 남긴다.
- 코드 설명은 클래스 책임, 변경 이유, 호출 흐름 위주로 제한한다.
- 단순 getter/setter, 단순 대입, Unity 기본 생명주기는 요청이 없으면 설명하지 않는다.
- 이미 `설계/` 문서에 있는 내용은 답변에서 다시 길게 설명하지 않고 문서명을 참조한다.
- 사용자가 "자세히" 요청하지 않으면 배경 설명보다 다음 작업과 결과를 우선 말한다.

## 문서 수정 토큰 절약 규칙

- 설계 문서를 수정할 때는 관련 문서 1개만 우선 수정한다.
- 여러 문서를 동시에 수정해야 하면 같은 설명을 반복하지 않고 한 문서에만 자세히 적는다.
- 다른 문서에서는 관련 문서명을 참조하는 방식으로 중복을 줄인다.
- 문서에 긴 코드 예시는 넣지 않는다. 필요하면 짧은 구조 예시나 의사코드만 사용한다.
- 로드맵, 개요, 세부 설계 문서가 같은 내용을 반복하지 않게 한다.

## Unity 씬/프리팹 토큰 절약 규칙

- 씬/프리팹은 텍스트 diff가 크므로 코드나 설정 파일로 해결 가능한 문제인지 먼저 확인한다.
- 씬/프리팹을 읽어야 하면 전체 YAML을 출력하지 않고 필요한 오브젝트명, 컴포넌트명, GUID만 검색한다.
- 씬/프리팹을 수정했다면 최종 답변에는 변경된 오브젝트와 컴포넌트만 요약한다.
- 씬/프리팹 변경 후 생성되는 대량 diff는 사용자가 요청하지 않으면 자세히 설명하지 않는다.

## 질문 답변 모드

- 사용자가 단순 질문을 하면 파일 검색 없이 현재 문맥과 `설계/` 문서 기준으로 먼저 답한다.
- 코드 확인이 필요한 질문일 때만 관련 파일을 검색한다.
- 질문 답변에서 전체 프로젝트 상태 점검을 임의로 시작하지 않는다.

## 설계와 코드 충돌 규칙

- 설계 문서와 현재 코드가 충돌하면 현재 코드를 우선한다.
- 충돌이 발견되면 설계 문서 갱신 필요성을 짧게 알린다.
- 설계 문서를 자동으로 고치기 전에 사용자의 요청 범위에 포함되는지 확인한다.

## 출력 길이 제한

- 기본 답변은 10줄 이내를 목표로 한다.
- 복잡한 작업 완료 보고도 가능하면 20줄을 넘기지 않는다.
- 긴 설명이 필요하면 먼저 요약을 제공하고, 사용자가 요청할 때 세부 내용을 확장한다.

## 목록 제한

- 후보, 문제, 개선안 목록은 기본 5개 이하로 제한한다.
- 더 많은 목록은 사용자가 요청할 때만 확장한다.
- 목록보다 결론이 중요한 경우 결론을 먼저 말한다.

## 설계 문서 사용 규칙

프로젝트 방향이 필요할 때는 `설계/` 폴더의 문서를 참고한다.

단, 모든 문서를 매번 읽지 않는다. 요청과 관련된 문서만 읽는다.

- 프로젝트 전체 방향: `설계/00_프로젝트_개요.md`
- 게임 기획: `설계/01_기획_방향.md`
- 시스템 구조: `설계/02_기술_설계.md`
- 캐릭터/전투: `설계/03_캐릭터_전투_설계.md`
- 맵/월드: `설계/04_맵_월드_설계.md`
- UI/아트: `설계/05_UI_아트_방향.md`
- 개발 방식: `설계/06_개발_운영_원칙.md`
- 일정/우선순위: `설계/07_로드맵.md`

## Unity 프로젝트 읽기 규칙

- 스크립트 작업은 우선 `Assets/` 아래 C# 파일만 대상으로 한다.
- 패키지 문제는 `Packages/manifest.json`부터 확인한다.
- 컴파일 에러는 Unity 로그 전체를 읽기보다 에러 코드와 파일 경로를 먼저 찾는다.
- 씬, 프리팹, 머티리얼, 모델 파일은 요청이 있거나 참조 문제가 명확할 때만 확인한다.
- `.meta` 파일은 GUID, 참조, 삭제/이동 문제 확인이 필요할 때만 읽는다.

## 구현 범위 원칙

큰 구조는 사용자가 직접 설계하고 구현하는 것을 우선한다.

AI가 완성 코드를 바로 제공하지 않는 영역:

- Player FSM 전체 구조
- Character Loader / Parts Combiner 전체 구조
- Behavior Tree 전체 엔진
- Weapon Combat System 핵심 구조
- Map Generator 핵심 아키텍처

위 영역에서는 먼저 다음 방식으로 돕는다.

1. 사용자의 설계 초안 확인
2. 책임 분리 검토
3. 의존성 검토
4. 확장성 문제 지적
5. 테스트 시나리오 제안
6. 필요한 경우 작은 예시 코드만 제공

## 코드 변경 규칙

- 기존 구조와 네이밍을 먼저 따른다.
- 요청 범위를 벗어난 리팩토링은 하지 않는다.
- 사용자가 수정한 것으로 보이는 변경은 되돌리지 않는다.
- 불필요한 새 추상화는 만들지 않는다.
- Unity 런타임 코드와 Editor 코드는 폴더와 의존성을 분리한다.
- 컴파일 가능한 작은 단위로 변경한다.

## 검증 규칙

가능하면 변경 후 아래 중 필요한 것만 실행한다.

- 관련 C# 컴파일 확인
- 에러 심볼 검색
- Unity 로그의 동일 에러 재검색
- 변경 파일의 문법 확인

검증이 불가능하면 최종 답변에 이유를 짧게 적는다.

## 최종 답변 규칙

최종 답변은 짧게 작성한다.

포함할 내용:

- 변경한 파일
- 핵심 변경 내용
- 검증 결과
- 남은 확인 사항

포함하지 않을 내용:

- 읽은 모든 파일 목록
- 긴 추론 과정
- 관련 없는 개선 제안
- 전체 파일 내용
